// Stub — implemented in Phase 5
// Maps SmoothedMetrics to CognitiveState labels

import type { CognitiveState, RawMetrics } from "@/lib/types";

export function resolveState(_metrics: RawMetrics): CognitiveState {
  throw new Error("resolveState not yet implemented — Phase 5");
}
