// Stub — implemented in Phase 7
// Rule-based commentary engine with 3 tone modes

import type { CognitiveState, CommentaryEntry, CommentaryMode } from "@/lib/types";

export function generateComment(
  _state: CognitiveState,
  _mode: CommentaryMode
): CommentaryEntry {
  throw new Error("generateComment not yet implemented — Phase 7");
}
